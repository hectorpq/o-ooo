name: agenda_ai
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  uuid: ^4.5.1
  provider: ^6.1.5+1
  table_calendar: ^3.2.0
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  http: ^1.5.0
  share_plus: ^11.1.0

  flutter_local_notifications: ^17.2.2
  permission_handler: ^11.3.1
  audioplayers: ^6.0.0
  timezone: ^0.9.4

  speech_to_text: ^7.3.0   # 🔥 actualizado, compatible con Flutter moderno
  shared_preferences: ^2.2.2  # Guardar tema
  flutter_colorpicker: ^1.0.3  # Selector de colores para categorías
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1   # 👈 añadimos este paquete

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png   # 👈 tu logo dentro de la app

flutter_icons:            # 👈 configuración del ícono de la app
  android: true
  ios: true
  image_path: "assets/logo.png"
